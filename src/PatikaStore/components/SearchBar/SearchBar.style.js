import { StyleSheet } from "react-native";

export default StyleSheet.create({

    input: {
        backgroundColor: '#eceff1',
        padding: 6,
        margin: 10,
        borderRadius: 10,
        color: 'black',
    },

});