import { StyleSheet } from "react-native";

export default StyleSheet.create({

    header: {
        color: '#840984',
        fontWeight: 'bold',
        margin: 10,
        fontSize: 30,
    },

});